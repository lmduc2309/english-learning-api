version: '3.8'

services:
  api:
    build: .
    ports:
      - "7474:80"
    environment:
      - VLLM_URL=${VLLM_URL}
      - VLLM_MODEL=${VLLM_MODEL}
      - PORT=80
    env_file:
      - .env
    restart: unless-stopped